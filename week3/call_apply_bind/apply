//apply() takes an array as its second argument and passes the members of that array as arguments
var obj = {
  x: 81,
  value_of_X: function() {
    return this.x;
  }
};
console.log(obj.value_of_X.apply(obj));
